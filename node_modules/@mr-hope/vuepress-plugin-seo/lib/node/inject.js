"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.addOGP = void 0;
const appendMeta = (meta, { name, content, attribute = ["article:", "og:"].some((type) => name.startsWith(type))
    ? "property"
    : "name", }) => {
    if (content)
        meta.push({ [attribute]: name, content });
};
const addOGP = (meta, content) => {
    for (const property in content)
        switch (property) {
            case "article:tag":
                content["article:tag"].forEach((tag) => appendMeta(meta, { name: "article:tag", content: tag }));
                break;
            case "og:locale:alternate":
                content["og:locale:alternate"].forEach((locale) => {
                    if (locale !== content["og:locale"])
                        appendMeta(meta, {
                            name: "og:locale:alternate",
                            content: locale,
                        });
                });
                break;
            default:
                if (content[property])
                    appendMeta(meta, {
                        name: property,
                        content: content[property],
                    });
        }
};
exports.addOGP = addOGP;
//# sourceMappingURL=inject.js.map
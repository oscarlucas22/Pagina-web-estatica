import { Feed } from "./feed";
import type { Context } from "@mr-hope/vuepress-types";
import type { ResolvedFeedOptionsMap } from "./options";
export declare class FeedGenerator {
    private context;
    private options;
    /** feed 生成器 */
    feedMap: Record<string, Feed>;
    constructor(context: Context, options: ResolvedFeedOptionsMap);
    addPages(localePath: string): void;
    generateFeed(): Promise<void>;
}

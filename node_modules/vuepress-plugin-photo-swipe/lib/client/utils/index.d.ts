import type { Item } from "photoswipe";
export declare const getImageInfo: (image: HTMLImageElement) => Item & {
    title: string;
};
export interface PhotoSwipeImages {
    elements: HTMLImageElement[];
    infos: Item[];
}
export declare const getImages: (selector: string) => Promise<PhotoSwipeImages>;
